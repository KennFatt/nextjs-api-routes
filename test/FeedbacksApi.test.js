describe("API Route: /api/feedbacks", () => {
  const API_ENDPOINT = "http://localhost:3000/api/feedbacks";

  describe("Method: GET", () => {
    it("Should be a JSON");
    it("Should have a `message` and `data` property");
    it("The `message` property should be a string");
    it("The `data` property should be an array");
    it("Should be and always returning 200 status");
  });
});
